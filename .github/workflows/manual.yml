# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      run: echo "Hello ${{ github.event.inputs.name }}"
      > group_ids = -1

> 

> telegram_token = 5327061627:AAHcnqfPuuwc3wozxanXn3KZcJqf27NvprM

>

> user_ids = 1622484167

>

> HEROKU_EMAIL = swami boy456@gmail.com

>

> HEROKU_API_KEY = 39674d64-2ec2-4e5f-a420-3c0aa982c52d

>

> HEROKU_APP_NAME = bloonw56> group_ids = -1

> 

> telegram_token = 5327061627:AAHcnqfPuuwc3wozxanXn3KZcJqf27NvprM

>

> user_ids = 1622484167

>

> HEROKU_EMAIL = swami boy456@gmail.com

>

> HEROKU_API_KEY = 39674d64-2ec2-4e5f-a420-3c0aa982c52d

>

> HEROKU_APP_NAME = bloonw56
